# Pure CPU environment dependencies (no CUDA / GPU needed)
#
# Recommended installation steps:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Note: be sure to install torch with the CPU index-url above, otherwise pip may by default pull the huge CUDA build.

torch>=2.0
numpy
scipy
scikit-learn
PyYAML
matplotlib

# Optional: used only to measure the inference memory (RSS) of each batch.
# When missing, adapt.py automatically falls back to 0, without affecting any functionality.
psutil
