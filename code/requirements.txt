# 纯 CPU 环境依赖(不需要 CUDA / GPU)
#
# 推荐安装步骤:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# 注意:请务必用上面的 CPU index-url 装 torch,否则 pip 默认可能拉到体积巨大的 CUDA 版。

torch>=2.0
numpy
scipy
scikit-learn
PyYAML
matplotlib

# 可选:仅用于统计每个 batch 的推理内存(RSS)。
# 缺失时 adapt.py 会自动降级为 0,不影响任何功能。
psutil
